<div class="container">
    <div class="row">
        <div class="col-3">
            <button onclick="history.back()">quay lai</button>
        </div>
        {{#each news}}
        <div class="col-9">
            <h3>{{this.title}}</h3>
            <h4>{{this.detail}}</h4>
        </div>
        {{/each}}
    </div>
</div>